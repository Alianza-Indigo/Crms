{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "Single-service deploy: one container runs api + worker + web + on-boot migrations (Dockerfile default APP=all). Provision the Postgres and Redis plugins and set the env vars from deploy/.env.prod.example / DEPLOY.md.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
